#!/usr/bin/env python3
# OPBOT - pure python3 IRC bot (bin/optbl)
#
# This file is placed in the Public Domain.

"create tables"

# import

from op.hdl import Handler

# runtime

h = Handler()
h.walk("opbot,op")

print("modnames = %s" % h.modnames)
print("names = %s" % h.names)
print("pkgnames = %s" % h.pkgnames)
